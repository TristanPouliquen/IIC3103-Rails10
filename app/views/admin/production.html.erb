<div class="col-md-offset-10 col-md-offset-1">
  <h1>Producir productos</h1>
  <p>Saldo: <%= number_with_delimiter(@saldo, delimiter: "." )%> CLP</p>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>SKU</th>
        <th>Nombre</th>
        <th>Precio unitario (CLP)</th>
        <th>Lote de produccion</th>
        <th>Tiempo de produccion</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <% @products.each do |product| %>
        <tr>
          <td><%= product["sku"] %></td>
          <td><%= product["name"] %></td>
          <td><%= product["unitPrice"] %></td>
          <td><%= product["lot"] %></td>
          <td><%= product["productionTime"] %></td>
          <td>
            <%= form_tag "/admin/produce", method: "post" do %>
              <input type="hidden" name="amount" value="<%= product['unitPrice']*product['lot'] %>">
              <input type="hidden" name="sku" value="<%= product['sku'] %>">
              <input type="hidden" name="lot" value="<%= product['lot'] %>">
              <input type="submit" class="btn btn-small btn-primary" <% if !product['productionOK'] %>disabled<% end%> value="Producir"></a>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
